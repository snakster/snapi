#pragma once

#include "../{{ module.name }}.h"

namespace {{ module.name }} {

class {{ service.name }}_impl : public {{ service.name }}
{
public:
    {% for fn in service.functions %}
    {{fn.return_type}} {{fn.name}}({{fn.args | cpp_args_as_str}}) override;
        
    {% endfor %}
};

} // namespace {{ module.name }}